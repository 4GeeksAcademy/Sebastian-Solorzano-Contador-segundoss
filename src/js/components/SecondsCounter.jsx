import React from "react";

const SecondsCounter = (props) => {

    return(
        <>
    <p>{props.seconds}</p>;
    <p>{props.seconds}</p>;
    <p>{props.seconds}</p>;
    </>
)};


export default SecondsCounter;